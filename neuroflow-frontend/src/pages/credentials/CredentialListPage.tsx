import { KeyRound } from 'lucide-react'
import { ComingSoon } from '@/components/common/ComingSoon'

// TODO(phase-5): see docs/15-security-and-credentials.md.
export default function CredentialListPage() {
  return (
    <ComingSoon
      icon={KeyRound}
      title="Credentials"
      description="Credential storage lands in Phase 5."
    />
  )
}
