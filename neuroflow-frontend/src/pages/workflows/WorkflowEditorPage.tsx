import { Workflow } from 'lucide-react'
import { ComingSoon } from '@/components/common/ComingSoon'

// TODO(phase-3): the React Flow canvas -- see docs/06-canvas-and-editor.md.
export default function WorkflowEditorPage() {
  return (
    <ComingSoon
      icon={Workflow}
      title="Workflow editor"
      description="The canvas lands in Phase 3. See docs/06-canvas-and-editor.md."
    />
  )
}
