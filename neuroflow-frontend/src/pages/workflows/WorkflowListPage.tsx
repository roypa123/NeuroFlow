import { Workflow } from 'lucide-react'
import { ComingSoon } from '@/components/common/ComingSoon'

// TODO(phase-3): real workflow list -- see docs/19-roadmap.md Phase 3.
export default function WorkflowListPage() {
  return (
    <ComingSoon
      icon={Workflow}
      title="Workflows"
      description="The canvas and workflow list land in Phase 3."
    />
  )
}
