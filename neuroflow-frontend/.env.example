# Local dev only. src/config/env.ts falls back to this when
# window.__NEUROFLOW_CONFIG__ is absent (that runtime config is emitted by
# the Docker entrypoint at container start -- see
# docs/18-deployment-and-operations.md #18.2). Not used in production
# builds either way.
VITE_API_BASE_URL=http://localhost:8000/api/v1
